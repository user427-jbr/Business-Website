[build]
  # The npm command to build the Astro site
  command = "npm run build"
  # The directory where Astro outputs the compiled static site
  publish = "dist"
  # The directory where Netlify functions (like the Resend email script) are stored
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"